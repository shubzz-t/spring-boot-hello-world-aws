version: 0.2

phases:
  install:
    commands:
      - echo Nothing to do in the install phase
  
  pre_build:
    commands:
      - echo Nothing to do in the pre build phase

  build:
    commands:
      - echo Build started on `date`
      - mvn install
  
  post_build:
    commands:
      - echo Build completed on `date`

artifacts:
 files:
  - '/root/.m2/repository/com/example/spring-boot-hello-world/1.0.2-SNAPSHOT/spring-boot-hello-world-1.0.2-SNAPSHOT.jar'